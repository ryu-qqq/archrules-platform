package com.ryuqqq.archrules.domain.fixture.compliant.domain.common.util;

/** ..common.util.. 패키지에 위치한 인터페이스 — 규칙 2(util 인터페이스만) compliant. */
public interface ValidUtil {
    String format(String input);
}
