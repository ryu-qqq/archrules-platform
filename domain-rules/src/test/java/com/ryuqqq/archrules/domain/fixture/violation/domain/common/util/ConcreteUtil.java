package com.ryuqqq.archrules.domain.fixture.violation.domain.common.util;

/** ..common.util.. 패키지에 위치한 구체 클래스 — 규칙 2(util 인터페이스만) violation. */
public class ConcreteUtil {
    public String format(String input) {
        return input.trim();
    }
}
